   {{ form_start(form, {'action': path('user_new', {'slug': slug })}) }}
   {{ form_widget(form) }}
    <input type="submit" value="Submit" class="btn btn-default pull-right" />
    {{ form_end(form) }}


